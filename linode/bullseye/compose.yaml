name: nodejs
services:
  scv-server:
    image: scvoice/scv-server:latest
    user: 1000:1000
    ports: 
      - "8080:8080"
    volumes:
      - type: volume
        source: scv-local
        target: /home/linode/scv-server/local
volumes:
  scv-local: 
  
